machine:
  node:
    version: 5.0.0

dependencies:
  post:
    - sudo pip install awscli

deployment:
  s3:
    branch: master
    commands:
      - npm run build
      - aws s3 sync ./dist s3://chrisledet.com.s3.amazonaws.com/projects/chip8.js
